<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sports.sales.mapper.CustomerMapper">

    <resultMap id="BaseResultMap" type="com.sports.sales.entity.Customer">
        <id column="customer_code" property="customerCode"/>
        <result column="customer_name" property="customerName"/>
        <result column="contact_name" property="contactName"/>
        <result column="address" property="address"/>
        <result column="postal_code" property="postalCode"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        customer_code, customer_name, contact_name, address, postal_code, phone, email, password, status, create_time, update_time
    </sql>

    <select id="selectList" resultMap="BaseResultMap" parameterType="com.sports.sales.dto.CustomerQueryDTO">
        SELECT <include refid="Base_Column_List"/>
        FROM customer
        <where>
            <if test="customerCode != null and customerCode != ''">
                AND customer_code LIKE CONCAT('%', #{customerCode}, '%')
            </if>
            <if test="customerName != null and customerName != ''">
                AND customer_name LIKE CONCAT('%', #{customerName}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="selectCount" resultType="java.lang.Long" parameterType="com.sports.sales.dto.CustomerQueryDTO">
        SELECT COUNT(*) FROM customer
        <where>
            <if test="customerCode != null and customerCode != ''">
                AND customer_code LIKE CONCAT('%', #{customerCode}, '%')
            </if>
            <if test="customerName != null and customerName != ''">
                AND customer_name LIKE CONCAT('%', #{customerName}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
        </where>
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM customer WHERE customer_code = #{customerCode}
    </select>

    <insert id="insert" parameterType="com.sports.sales.entity.Customer">
        INSERT INTO customer (customer_code, customer_name, contact_name, address, postal_code, phone, email, password, status)
        VALUES (#{customerCode}, #{customerName}, #{contactName}, #{address}, #{postalCode}, #{phone}, #{email}, #{password}, #{status})
    </insert>

    <update id="updateByCode" parameterType="com.sports.sales.entity.Customer">
        UPDATE customer
        <set>
            <if test="customerName != null">customer_name = #{customerName},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="address != null">address = #{address},</if>
            <if test="postalCode != null">postal_code = #{postalCode},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="password != null">password = #{password},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE customer_code = #{customerCode}
    </update>

    <delete id="deleteByCode">
        DELETE FROM customer WHERE customer_code = #{customerCode}
    </delete>

    <update id="updatePasswordByCode">
        UPDATE customer SET password = #{password} WHERE customer_code = #{customerCode}
    </update>
</mapper>
